import React from 'react';
import Logo from '../../images/LOGO.png';

export const LogoPlaceholder = (props) => {
    return (
        <div id='LogoPlaceholder'>
            <img src={ Logo } alt=""/>
        </div>
    );
}